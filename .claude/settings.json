{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && { if [ -f .claude/.adhd-always ]; then echo \"=== ADHD MODE ACTIVE (always-on). Rules below apply to every response this session. Say \\\"stop adhd mode\\\" to turn off for this session; delete .claude/.adhd-always to turn it off for good. ===\"; sed '1{/^---$/!q};1,/^---$/d' .claude/skills/i-have-adhd/SKILL.md; echo; fi; bash ./life status; } 2>&1 || true",
            "timeout": 30,
            "statusMessage": "Loading your Life OS"
          }
        ]
      }
    ]
  }
}
